---
title: "Go语言-测试与基准测试"
course: Go语言
chapter: 工程质量
difficulty: BASIC
tags: [Go语言, testing, benchmark, 表驱动测试, test]
aliases: [Go Testing, Benchmark, Table-Driven Tests]
source: "Go官方文档 testing包; Go Blog: TableDrivenTests; Effective Go"
updated_at: 2026-05-02
---

## 核心定义

""Go的测试框架内置于testing包。测试函数签名：func TestXxx(t *testing.T)。运行命令：go test ./...。基准测试函数签名：func BenchmarkXxx(b *testing.B),运行方式go test -bench=.。表驱动测试(table-driven tests)是Go社区的标准风格：创建[]struct{name, input, want}切片，循环调用t.Run执行子测试。go test -run=REGEX可筛选特定测试。

## 表驱动测试范式

""表驱动测试将测试用例数据与测试逻辑分离：tests := []struct{name string; input Type; want Type}{{...},{...}}; for _, tt := range tests { t.Run(tt.name, func(t *testing.T) { got := Func(tt.input); if got != tt.want { t.Errorf('...') } })}。每个子测试独立运行，支持-parallel并行。t.Cleanup可以注册清理函数。

## 关键结论

""1. 测试文件以_test.go结尾 2. 基准测试需要ResetTimer排除准备时间 3. TestMain(m *testing.M)可做全局设置/清理 4. golden file测试常用于复杂输出 5. 覆盖率profile: go test -coverprofile=coverage.out 6. 竞态检测: go test -race

## 关联知识点

""[[Go语言-错误处理哲学]] [[软件工程-软件测试策略]] [[Go语言-Go运行时调度器GPM]]
